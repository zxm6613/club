<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zxm.club.subject.infra.basic.mapper.SubjectJudgeDao">

    <resultMap type="com.zxm.club.subject.infra.basic.entity.SubjectJudge" id="SubjectJudgeMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="createdBy" column="CREATED_BY" jdbcType="VARCHAR"/>
        <result property="createdTime" column="CREATED_TIME" jdbcType="TIMESTAMP"/>
        <result property="updatedBy" column="UPDATED_BY" jdbcType="VARCHAR"/>
        <result property="updatedTime" column="UPDATED_TIME" jdbcType="TIMESTAMP"/>
        <result property="subjectId" column="subject_id" jdbcType="INTEGER"/>
        <result property="isCorrect" column="is_correct" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="SubjectJudgeMap">
        select
          id, CREATED_BY, CREATED_TIME, UPDATED_BY, UPDATED_TIME, subject_id, is_correct
        from subject_judge
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="SubjectJudgeMap">
        select
          id, CREATED_BY, CREATED_TIME, UPDATED_BY, UPDATED_TIME, subject_id, is_correct
        from subject_judge
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="createdBy != null and createdBy != ''">
                and CREATED_BY = #{createdBy}
            </if>
            <if test="createdTime != null">
                and CREATED_TIME = #{createdTime}
            </if>
            <if test="updatedBy != null and updatedBy != ''">
                and UPDATED_BY = #{updatedBy}
            </if>
            <if test="updatedTime != null">
                and UPDATED_TIME = #{updatedTime}
            </if>
            <if test="subjectId != null">
                and subject_id = #{subjectId}
            </if>
            <if test="isCorrect != null">
                and is_correct = #{isCorrect}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from subject_judge
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="createdBy != null and createdBy != ''">
                and CREATED_BY = #{createdBy}
            </if>
            <if test="createdTime != null">
                and CREATED_TIME = #{createdTime}
            </if>
            <if test="updatedBy != null and updatedBy != ''">
                and UPDATED_BY = #{updatedBy}
            </if>
            <if test="updatedTime != null">
                and UPDATED_TIME = #{updatedTime}
            </if>
            <if test="subjectId != null">
                and subject_id = #{subjectId}
            </if>
            <if test="isCorrect != null">
                and is_correct = #{isCorrect}
            </if>
        </where>
    </select>
    <select id="queryBySubjectId" resultMap="SubjectJudgeMap">
        select
            id, CREATED_BY, CREATED_TIME, UPDATED_BY, UPDATED_TIME, subject_id, is_correct
        from subject_judge
        where subject_id = #{id}
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into subject_judge(CREATED_BY, CREATED_TIME, UPDATED_BY, UPDATED_TIME, subject_id, is_correct)
        values (#{createdBy}, #{createdTime}, #{updatedBy}, #{updatedTime}, #{subjectId}, #{isCorrect})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into subject_judge(CREATED_BY, CREATED_TIME, UPDATED_BY, UPDATED_TIME, subject_id, is_correct)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.createdBy}, #{entity.createdTime}, #{entity.updatedBy}, #{entity.updatedTime}, #{entity.subjectId}, #{entity.isCorrect})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into subject_judge(CREATED_BY, CREATED_TIME, UPDATED_BY, UPDATED_TIME, subject_id, is_correct)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.createdBy}, #{entity.createdTime}, #{entity.updatedBy}, #{entity.updatedTime}, #{entity.subjectId}, #{entity.isCorrect})
        </foreach>
        on duplicate key update
        CREATED_BY = values(CREATED_BY),
        CREATED_TIME = values(CREATED_TIME),
        UPDATED_BY = values(UPDATED_BY),
        UPDATED_TIME = values(UPDATED_TIME),
        subject_id = values(subject_id),
        is_correct = values(is_correct)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update subject_judge
        <set>
            <if test="createdBy != null and createdBy != ''">
                CREATED_BY = #{createdBy},
            </if>
            <if test="createdTime != null">
                CREATED_TIME = #{createdTime},
            </if>
            <if test="updatedBy != null and updatedBy != ''">
                UPDATED_BY = #{updatedBy},
            </if>
            <if test="updatedTime != null">
                UPDATED_TIME = #{updatedTime},
            </if>
            <if test="subjectId != null">
                subject_id = #{subjectId},
            </if>
            <if test="isCorrect != null">
                is_correct = #{isCorrect},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from subject_judge where id = #{id}
    </delete>
    <delete id="deleteBySubjectId">
        delete from subject_judge where subject_id = #{id}
    </delete>

</mapper>

