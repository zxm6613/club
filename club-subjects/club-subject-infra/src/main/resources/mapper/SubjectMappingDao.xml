<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zxm.club.subject.infra.basic.mapper.SubjectMappingDao">

    <resultMap type="com.zxm.club.subject.infra.basic.entity.SubjectMapping" id="SubjectMappingMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="createdBy" column="CREATED_BY" jdbcType="VARCHAR"/>
        <result property="createdTime" column="CREATED_TIME" jdbcType="TIMESTAMP"/>
        <result property="updatedBy" column="UPDATED_BY" jdbcType="VARCHAR"/>
        <result property="updatedTime" column="UPDATED_TIME" jdbcType="TIMESTAMP"/>
        <result property="categoryId" column="category_id" jdbcType="INTEGER"/>
        <result property="subjectId" column="subject_id" jdbcType="INTEGER"/>
        <result property="labelId" column="label_id" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="SubjectMappingMap">
        select
          id, CREATED_BY, CREATED_TIME, UPDATED_BY, UPDATED_TIME, category_id, subject_id, label_id
        from subject_mapping
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="SubjectMappingMap">
        select
          id, CREATED_BY, CREATED_TIME, UPDATED_BY, UPDATED_TIME, category_id, subject_id, label_id
        from subject_mapping
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="createdBy != null and createdBy != ''">
                and CREATED_BY = #{createdBy}
            </if>
            <if test="createdTime != null">
                and CREATED_TIME = #{createdTime}
            </if>
            <if test="updatedBy != null and updatedBy != ''">
                and UPDATED_BY = #{updatedBy}
            </if>
            <if test="updatedTime != null">
                and UPDATED_TIME = #{updatedTime}
            </if>
            <if test="categoryId != null">
                and category_id = #{categoryId}
            </if>
            <if test="subjectId != null">
                and subject_id = #{subjectId}
            </if>
            <if test="labelId != null">
                and label_id = #{labelId}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from subject_mapping
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="createdBy != null and createdBy != ''">
                and CREATED_BY = #{createdBy}
            </if>
            <if test="createdTime != null">
                and CREATED_TIME = #{createdTime}
            </if>
            <if test="updatedBy != null and updatedBy != ''">
                and UPDATED_BY = #{updatedBy}
            </if>
            <if test="updatedTime != null">
                and UPDATED_TIME = #{updatedTime}
            </if>
            <if test="categoryId != null">
                and category_id = #{categoryId}
            </if>
            <if test="subjectId != null">
                and subject_id = #{subjectId}
            </if>
            <if test="labelId != null">
                and label_id = #{labelId}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into subject_mapping(CREATED_BY, CREATED_TIME, UPDATED_BY, UPDATED_TIME, category_id, subject_id, label_id)
        values (#{createdBy}, #{createdTime}, #{updatedBy}, #{updatedTime}, #{categoryId}, #{subjectId}, #{labelId})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into subject_mapping(CREATED_BY, CREATED_TIME, UPDATED_BY, UPDATED_TIME, category_id, subject_id, label_id)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.createdBy}, #{entity.createdTime}, #{entity.updatedBy}, #{entity.updatedTime}, #{entity.categoryId}, #{entity.subjectId}, #{entity.labelId})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into subject_mapping(CREATED_BY, CREATED_TIME, UPDATED_BY, UPDATED_TIME, category_id, subject_id, label_id)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.createdBy}, #{entity.createdTime}, #{entity.updatedBy}, #{entity.updatedTime}, #{entity.categoryId}, #{entity.subjectId}, #{entity.labelId})
        </foreach>
        on duplicate key update
        CREATED_BY = values(CREATED_BY),
        CREATED_TIME = values(CREATED_TIME),
        UPDATED_BY = values(UPDATED_BY),
        UPDATED_TIME = values(UPDATED_TIME),
        category_id = values(category_id),
        subject_id = values(subject_id),
        label_id = values(label_id)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update subject_mapping
        <set>
            <if test="createdBy != null and createdBy != ''">
                CREATED_BY = #{createdBy},
            </if>
            <if test="createdTime != null">
                CREATED_TIME = #{createdTime},
            </if>
            <if test="updatedBy != null and updatedBy != ''">
                UPDATED_BY = #{updatedBy},
            </if>
            <if test="updatedTime != null">
                UPDATED_TIME = #{updatedTime},
            </if>
            <if test="categoryId != null">
                category_id = #{categoryId},
            </if>
            <if test="subjectId != null">
                subject_id = #{subjectId},
            </if>
            <if test="labelId != null">
                label_id = #{labelId},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from subject_mapping where id = #{id}
    </delete>

</mapper>

